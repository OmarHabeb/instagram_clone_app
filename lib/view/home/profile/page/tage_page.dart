import 'package:flutter/material.dart';

class TagePage extends StatelessWidget {
  const TagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(

      body: Center(
        child: Text("tage"),
      ),
    );
  }
}